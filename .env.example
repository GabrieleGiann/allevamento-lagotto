# Copia questo file in ".env.local" e compila i valori per attivare WordPress headless.
# Finché è vuoto/assente, il sito usa i dati statici locali (funziona comunque).

# URL base dell'installazione WordPress (CMS), senza slash finale.
# Esempio: https://cms.iltuodominio.it
WORDPRESS_API_URL=
